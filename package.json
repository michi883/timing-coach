{
  "name": "timing-coach",
  "version": "1.0.0",
  "description": "AI-powered language timing coach for 15 languages",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy --only hosting"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}